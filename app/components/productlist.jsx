import React from "react";

//Template, Empty JS
export default function ProductList( {productListRef} ) {
  return (
    <div ref={productListRef}  className="flex flex-col items-center justify-center min-h-screen bg-cover bg-center w-full bg-white" >
    </div>
  );
}